FROM nginx:alpine

# eski configni o‘chirib tashlaymiz
RUN rm /etc/nginx/conf.d/default.conf

# custom nginx.conf
COPY nginx.conf /etc/nginx/nginx.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
